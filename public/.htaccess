# Specitas Group – Apache SPA rewrite for SiteGround
DirectoryIndex site.html index.html

# Never let SiteGround / browsers cache HTML entry documents
<IfModule mod_headers.c>
  Header always set Cache-Control "no-cache, no-store, must-revalidate, max-age=0"
  Header always set Pragma "no-cache"
  Header always set Expires "0"
</IfModule>

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Force HTTPS when available
  RewriteCond %{HTTPS} !=on
  RewriteCond %{HTTP:X-Forwarded-Proto} !https
  RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

  # Business & Beyond archive recovery (served from /tcbab/)
  RewriteCond %{HTTP_HOST} ^(www\.)?tcbab\.specitas\.com$ [NC,OR]
  RewriteCond %{HTTP_HOST} ^(www\.)?tcbab\.com$ [NC]
  RewriteCond %{REQUEST_URI} !^/tcbab/
  RewriteRule ^(.*)$ /tcbab/$1 [L]

  # Block leftover WordPress / PHP attack surfaces
  RewriteRule ^wp-admin(/|$) - [F,L]
  RewriteRule ^wp-content(/|$) - [F,L]
  RewriteRule ^wp-includes(/|$) - [F,L]
  RewriteRule ^wp-.*\.php$ - [F,L]
  RewriteRule ^xmlrpc\.php$ - [F,L]
  RewriteRule ^wp_auto_login_.*\.php$ - [F,L]
  RewriteRule \.php$ - [F,L]

  # Serve existing static files and folders as-is
  RewriteRule ^index\.html$ - [L]
  RewriteRule ^site\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  # React Router fallback
  RewriteRule . /site.html [L]
</IfModule>

# Deny direct access to PHP leftovers
<FilesMatch "\.(php|PHP)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>

# Cache hashed static assets only (not HTML)
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
  ExpiresByType image/png "access plus 1 month"
  ExpiresByType image/jpeg "access plus 1 month"
  ExpiresByType image/svg+xml "access plus 1 month"
  ExpiresByType video/mp4 "access plus 1 month"
  ExpiresByType text/html "access plus 0 seconds"
</IfModule>
